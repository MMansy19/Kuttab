# IMPORTANT: When deploying to Vercel, set these variables in your Vercel project settings
# Do not commit sensitive information to version control

DATABASE_URL="postgresql://postgres:Mahmoud_132003@localhost:5432/kottab_db?schema=public"

# Add connection pooling parameters for better performance with serverless
# The pgbouncer=true parameter enables connection pooling
# The connect_timeout=15 parameter prevents hanging connections

# Authentication Secrets - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="Hha0zIBh1E1BLgtBNk7axqglfl4AR5bdw+U5JjA0OoI="
NEXTAUTH_URL="https://kuttab.vercel.app"

# Production Settings
NODE_ENV="production"
