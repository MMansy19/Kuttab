# IMPORTANT: Don't use this file directly on Vercel
# Instead, set these environment variables in the Vercel project settings

# Production Database URL - Use a serverless-compatible PostgreSQL URL (Neon recommended)
# DATABASE_URL="postgresql://<user>:<password>@<hostname>/<database>?sslmode=require"

# Authentication Secrets - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# NEXTAUTH_SECRET="generate-a-strong-unique-secret-here"
# NEXTAUTH_URL="https://kuttab.vercel.app"

# Production Settings
NODE_ENV="production"
