{
  "version": 2,
  "buildCommand": "npx prisma generate && npm install cross-env -g && npm run build:vercel",
  "devCommand": "npm run dev",
  "installCommand": "npm install && npx prisma generate",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "false"
  }
}
