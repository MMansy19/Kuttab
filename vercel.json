{
  "version": 2,
  "buildCommand": "npx prisma generate && npx cross-env PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1 node scripts/build-info.js && node scripts/prisma-vercel.js && npm run prepare:vercel && npm run prisma:fix && npm run auth:fix && node scripts/pre-build-fix.js && next build && node scripts/fix-route-types.js && node scripts/build/vercel-build.js",
  "devCommand": "npm run dev",
  "installCommand": "npm install && npx prisma generate",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "false"
  }
}
